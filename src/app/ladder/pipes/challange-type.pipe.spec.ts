import { ChallangeTypePipe } from './challange-type.pipe';

describe('ChallangeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ChallangeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
